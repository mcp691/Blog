<hr>
<h3><%= link_to post.title, post %></h3>
<p>Posted on <%= post.created_at.strftime("%d/%m/%Y") %> at <%= post.created_at.strftime("%H:%M") %></p>
<%= post.content %>
<br><br>
<%= link_to 'Show', post %>
<%= link_to 'Edit', edit_post_path(post) %>
<%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %>
